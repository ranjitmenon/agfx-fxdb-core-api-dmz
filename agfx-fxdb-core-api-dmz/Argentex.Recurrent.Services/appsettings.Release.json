{
  "ConnectionStrings": {
    "FXDB1": "#{ConnectionStrings:FXDBConnectionString}",
    "NLogWrapperDB": "#{ConnectionStrings:NLogWrapperConnectionString}",
    "IdentityDB": "#{ConnectionStrings:IdentityConnectionString}"
  },

  "AppSettings": {
    "WebsiteName": "#{WebsiteName}",
    "WebApplicationName": "#{WebApplicationName}"
  },

  "FixUrl": {
    "BaseUrl": "#{Fix:BaseUrl}",
    "QuoteRequest": "/api/fix/quote",
    "OrderRequest": "/api/fix/newOrderSingle"
  }
}
